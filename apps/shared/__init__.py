"""Shared Python packages used by both `apps/bot/` and `apps/scheduler/`.

Top-level marker so `import shared.publishers...` works when /app/shared/
is on PYTHONPATH (which is what `apps/*/Dockerfile` arranges).
"""
