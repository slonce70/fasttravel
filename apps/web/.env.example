# Public API base URL. The Next.js client uses this directly via fetch.
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_TELEGRAM_CHANNEL_URL=https://t.me/testtyhhh

# Production/preview overrides are configured in wrangler.jsonc vars.
