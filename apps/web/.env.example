# Public API base URL. The Next.js client uses this directly via fetch.
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production override is configured in wrangler.toml [env.production.vars].
